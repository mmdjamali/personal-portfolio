import Icon from "@/components/icon";

const Loading = () => {
  return (
    <div className="grid h-full w-full place-items-center bg-background">
      <Icon name="Crown" className="h-[50px] text-foreground" />
    </div>
  );
};

export default Loading;
